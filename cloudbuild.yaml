steps:
  # Instal
  - name: "gcr.io/cloud-builders/npm"
    args: ["install"]

  # # Run
  # - name: 'gcr.io/cloud-builders/npm'
  #   args: ['run']

  # Build
  - name: "gcr.io/cloud-builders/npm"
    args: ["run", "build"]

  # Build
  - name: "gcr.io/authentication-app-2daff/firebase"
    args: ["deploy"]
